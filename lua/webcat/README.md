# webcat.nvim Module

Reserved for the optional Neovim layer.

This module must remain a thin consumer of the CLI's `schema:1` JSON output.
It should not parse raw JUnit, PIT, JaCoCo, or Web-CAT submitter output.
